{
	"name": "moje-naselje",
	"version": "1.0.0",
	"description": "Cloudflare Worker that collects Belgrade news from Google News RSS and publishes to Viber Channel",
	"main": "src/index.ts",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types",
		"test": "vitest run"
	},
	"dependencies": {
		"fast-xml-parser": "^4.5.3"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250214.0",
		"typescript": "^5.7.3",
		"vitest": "^3.0.5",
		"wrangler": "^3.109.2"
	}
}
